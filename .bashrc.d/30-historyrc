# .bashrc.d/historyrc
# /* vim: set filetype=rc : */

# from https://blog.sanctum.geek.nz/better-bash-history/

# Append history instead of rewriting it
shopt -s histappend

# Allow a larger history file
HISTFILESIZE=1000000
HISTSIZE=1000000

# Don’t store specific lines
# space at start
# duplicate commands
HISTCONTROL=ignoreboth
# remove the use of certain commands from your history
HISTIGNORE='ls:bg:fg:history:pass'

# Record timestamps, full date and time
HISTTIMEFORMAT='%F %T '

# Use one command per line
# commands entered on more than one line will get stored on one line
shopt -s cmdhist

# Store history immediately
PROMPT_COMMAND='history -a'

